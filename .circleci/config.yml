version: 2

jobs:
  setup:
    docker:
      - image: circleci/node:8.12.0

    working_directory: ~/elm

    steps:
      - checkout

      - restore_cache:
          keys:
          - elm-0.18-libsysconfcpus-dependencies-{{ checksum "scripts/ci/ensure_libsysconfcpus.sh" }}

      - run: ./scripts/ci/ensure_libsysconfcpus.sh

      - save_cache:
          paths:
            - sysconfcpus
          key: elm-0.18-libsysconfcpus-dependencies-{{ checksum "scripts/ci/ensure_libsysconfcpus.sh" }}

      - restore_cache:
          keys:
          - elm-0.18-node_modules-dependencies-{{ checksum "package.json" }}

      - run: npm install
      - run: ./scripts/ci/replace_elm_make.sh 1

      - save_cache:
          paths:
            - node_modules
          key: elm-0.18-node_modules-dependencies-{{ checksum "package.json" }}

      - persist_to_workspace:
          root: ~/
          paths: ./*

  test:
    docker:
      - image: circleci/node:8.12.0

    working_directory: ~/elm

    steps:
      - attach_workspace:
          at: ~/

      - restore_cache:
          keys:
          - elm-0.18-dependencies-{{ checksum "elm-package.json" }}-{{ checksum "tests/elm-package.json" }}

      - run: npm run test

      - save_cache:
          paths:
            - elm-stuff
            - tests/elm-stuff
          key: elm-0.18-dependencies-{{ checksum "elm-package.json" }}-{{ checksum "tests/elm-package.json" }}

      - persist_to_workspace:
          root: ~/
          paths: ./*

  build:
    docker:
      - image: circleci/node:8.12.0

    working_directory: ~/elm

    steps:
      - attach_workspace:
          at: ~/

      - run: npm run build

workflows:
  version: 2

  test-and-build:
    jobs:
      - setup

      - test:
          requires:
            - setup

      - build:
          requires:
            - test
